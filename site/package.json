{
  "name": "plateforme-site",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && tsc -p tsconfig.scripts.json && vite build && node --experimental-strip-types scripts/prerendre.ts",
    "test": "node --experimental-strip-types --test src/*.test.ts",
    "preview": "vite preview"
  },
  "dependencies": {
    "maplibre-gl": "^5.9.0",
    "pmtiles": "^4.4.0"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "^5.9.0",
    "vite": "^7.1.0"
  }
}
